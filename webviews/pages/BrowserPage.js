import App from "../components/BrowserPage.svelte";

//Ignore Error Below 
const app = new App({
  // @ts-ignore
  // eslint-disable-next-line no-undef
  target: document.body,
});

export default app;